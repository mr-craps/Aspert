<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Aspert.ViewModels"
             x:Class="Aspert.MainPage"
             x:DataType="vm:MainPageViewModel">
    <Grid BackgroundColor="#c0ccda" 
          ColumnDefinitions="*" 
          RowDefinitions="50,3*,*,3*,50">
        <Grid.BindingContext>
            <vm:MainPageViewModel/>
        </Grid.BindingContext>
        <ImageButton Grid.Row="0"
            BackgroundColor="#c0ccda"
            Command="{Binding Menu}"
            HorizontalOptions="Start"
            Source="menug_aspert.png"/>
        <Image Grid.Row="1"
               Grid.RowSpan="3"
               Aspect="AspectFill"
               Source="bg3_aspert.png"/>
        <Image Grid.Row="2"
               Source="nuevologo_aspert.png"/>
        <Label Grid.Row="3"
               FontAttributes="Bold"
               HorizontalOptions="Center"
               Text="ASPERT" 
               TextColor="#3d51a9"/>
        <ImageButton Grid.Row="4"
                     HeightRequest="50"
                     BackgroundColor="#c0ccda"
                     Command="{Binding Configuration}"
                     Source="user_aspert.png" 
                     HorizontalOptions="End"/>
    </Grid>
</ContentPage>